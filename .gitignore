__pycache__/
*.py[cod]
# Build artefacts if the vendored mamba-ssm tree is ever compiled in place
# (uv builds it out-of-tree); --check writes .mamba-ssm-check and removes it.
vendor/*/build/
vendor/*/*.egg-info/
vendor/.mamba-ssm-check/
*.log
*.mat
PreprocessedData
PreTrainedModels
runs
temp
*.out
.venv
.uv_cache
logs/
model_outputs/
# Local Claude state stays out, but the checked-in skills travel with the repo
.claude/*
!.claude/skills/
.configs
.DS_Store
